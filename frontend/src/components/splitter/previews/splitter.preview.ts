import { SkinElement } from "../../../models/skin/element";

const elements: SkinElement[] = [
    {
        id: "splitter",
        label: "Splitter",
        selector: "#splitter",
    },
    {
        id: "comparison_mode",
        label: "Comparison Mode",
        selector: ".comparison-mode",
    },
];

export default elements;
